# Universal scientific & deep learning core for CPU CI and local runtime
# Note: On standard PyPI, these versions resolve to standard CPU wheels.
# On NVIDIA GPU workstations, install CUDA wheels as documented in requirements.txt.
numpy>=2.0.0
pandas>=2.2.0
matplotlib>=3.8.0
scikit-learn>=1.4.0
pyyaml>=6.0.0
optuna>=3.6.0
tabulate>=0.9.0
pillow>=10.0.0
tensorboard>=2.15.0
torch>=2.2.0
torchvision>=0.17.0
